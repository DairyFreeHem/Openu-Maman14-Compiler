

/*Run first-pass of compiler,
 *will assign memory length to code,
 *get all Labels, directive data, and code data
 and send out data to second-pass to complete full code read*/
void firstPass(char *filename);